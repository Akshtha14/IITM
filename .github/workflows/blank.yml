name: IITM Email Verification

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2081878@ds.study.iitm.ac.in
        run: echo "Hello, world!"

